const isValidId = require("./isValidId");
const authenficate = require("./authenficate");
const upload = require("./upload");
const imageCompression = require("./imageCompression");

module.exports = {
  isValidId,
  authenficate,
  upload,
  imageCompression,
};
